import { AnimatePresence } from "framer-motion";

const StairTransition = () => {
    return (
        <AnimatePresence>
            StairTransition
        </AnimatePresence>
    );
};

export default StairTransition;